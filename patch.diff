diff --git a/app/src/main/java/net/rpcsx/MainActivity.kt b/app/src/main/java/net/rpcsx/MainActivity.kt
index 784d73f..ff49904 100644
--- a/app/src/main/java/net/rpcsx/MainActivity.kt
+++ b/app/src/main/java/net/rpcsx/MainActivity.kt
@@ -28,6 +28,7 @@ class MainActivity : ComponentActivity() {
         unregisterGamepadEventListener = listenGamepadEvents(this)
 
         GeneralSettings.init(this)
+        GameDirectoryRepository.load()
 
         if (!RPCSX.initialized) {
             Permission.PostNotifications.requestPermission(this)
diff --git a/app/src/main/java/net/rpcsx/PrecompilerService.kt b/app/src/main/java/net/rpcsx/PrecompilerService.kt
index 11e6cc5..657e63e 100644
--- a/app/src/main/java/net/rpcsx/PrecompilerService.kt
+++ b/app/src/main/java/net/rpcsx/PrecompilerService.kt
@@ -125,6 +125,14 @@ class PrecompilerService : Service() {
 
         if (isFwInstall) {
             FirmwareRepository.progressChannel.value = installProgress
+        } else {
+            // Show a "processing" placeholder card in the game grid while the
+            // file (ISO/PKG) is copied/installed, mirroring the folder-add flow
+            // (FileUtil.installPackages). The placeholder is keyed to this
+            // progress and removed once it finishes (ProgressRepository.cancel
+            // -> GameRepository.clearProgress); the real game is added by the
+            // native collectGameInfo call inside install().
+            GameRepository.createGameInstallEntry(installProgress)
         }
 
         try {
diff --git a/app/src/main/java/net/rpcsx/RPCSXActivity.kt b/app/src/main/java/net/rpcsx/RPCSXActivity.kt
index a2bbb86..a47eaef 100644
--- a/app/src/main/java/net/rpcsx/RPCSXActivity.kt
+++ b/app/src/main/java/net/rpcsx/RPCSXActivity.kt
@@ -20,6 +20,7 @@ import androidx.core.view.updateLayoutParams
 import net.rpcsx.databinding.ActivityRpcs3Binding
 import net.rpcsx.dialogs.AlertDialogQueue
 import net.rpcsx.overlay.State
+import net.rpcsx.utils.GeneralSettings
 import net.rpcsx.utils.InputBindingPrefs
 import kotlin.concurrent.thread
 import kotlin.math.abs
@@ -50,6 +51,21 @@ class RPCSXActivity : Activity() {
         RPCSX.instance.getMaxVirtualPads().coerceIn(1, MaxGamepadPlayers)
     }
 
+    // Port the on-screen touch overlay is forced to occupy, but only while no
+    // physical controller is connected (see effectiveOverlaySlot). With any
+    // controller present the overlay reverts to slot 0 and coexists with the
+    // main pad through mergedSlot0State, exactly as before.
+    private val forcedOverlaySlot by lazy {
+        ((GeneralSettings["overlay_forced_slot"] as? Int) ?: 0).coerceIn(0, maxVirtualPads - 1)
+    }
+    // Slot the overlay drove on the previous send, so a connect/disconnect that
+    // moves it (e.g. forced slot 2 -> slot 0 when a pad shows up) can release
+    // the buttons it left held on the slot it is leaving.
+    private var lastOverlaySlot = 0
+
+    private fun effectiveOverlaySlot() =
+        if (connectedGamepadCount() == 0) forcedOverlaySlot else 0
+
     private val inputDeviceListener = object : InputManager.InputDeviceListener {
         override fun onInputDeviceAdded(deviceId: Int) {
             onGamepadConnectionChanged()
@@ -91,7 +107,11 @@ class RPCSXActivity : Activity() {
         // too instead of it calling into native directly — otherwise
         // whichever side last sent a full State() would silently drop the
         // other's currently-held buttons.
-        binding.padOverlay.onPadStateChanged = { sendPadData(0, binding.padOverlay.currentState) }
+        binding.padOverlay.onPadStateChanged = {
+            val slot = effectiveOverlaySlot()
+            lastOverlaySlot = slot
+            sendPadData(slot, binding.padOverlay.currentState)
+        }
 
         applyOverlayAutoVisibility(connectedGamepadCount() > 0)
 
@@ -182,6 +202,15 @@ class RPCSXActivity : Activity() {
     private fun connectedGamepadCount() = GamepadRepository.slots.size
 
     private fun onGamepadConnectionChanged() {
+        // A pad connecting/disconnecting can move the overlay between its forced
+        // slot and slot 0. Release whatever it was holding on the slot it leaves
+        // so those buttons don't stay stuck on the now-unused virtual pad.
+        val newSlot = effectiveOverlaySlot()
+        if (newSlot != lastOverlaySlot) {
+            sendPadData(lastOverlaySlot, State())
+            lastOverlaySlot = newSlot
+            sendPadData(newSlot, binding.padOverlay.currentState)
+        }
         applyOverlayAutoVisibility(connectedGamepadCount() > 0)
     }
 
diff --git a/app/src/main/java/net/rpcsx/ui/navigation/AppNavHost.kt b/app/src/main/java/net/rpcsx/ui/navigation/AppNavHost.kt
index c8ac43b..52cf3c7 100644
--- a/app/src/main/java/net/rpcsx/ui/navigation/AppNavHost.kt
+++ b/app/src/main/java/net/rpcsx/ui/navigation/AppNavHost.kt
@@ -80,6 +80,7 @@ import kotlinx.coroutines.launch
 import net.rpcsx.BuildConfig
 import net.rpcsx.EmulatorState
 import net.rpcsx.FirmwareRepository
+import net.rpcsx.GameDirectoryRepository
 import net.rpcsx.PrecompilerService
 import net.rpcsx.PrecompilerServiceAction
 import net.rpcsx.ProgressRepository
@@ -100,6 +101,7 @@ import net.rpcsx.ui.channels.channelsToUiText
 import net.rpcsx.ui.channels.uiTextToChannel
 import net.rpcsx.ui.channels.uiTextToChannels
 import net.rpcsx.ui.drivers.GpuDriversScreen
+import net.rpcsx.ui.games.GameDirectoriesScreen
 import net.rpcsx.ui.games.GamesScreen
 import net.rpcsx.ui.settings.AdvancedSettingsScreen
 import net.rpcsx.ui.settings.ControllerSettings
@@ -205,7 +207,8 @@ fun AppNavHost() {
             GamesDestination(
                 navigateToSettings = { navigateTo("settings") },
                 navigateToControls = { navigateTo("controls") },
-                drawerState
+                drawerState = drawerState,
+                navigateToDirectories = { navigateTo("game_directories") }
             )
         }
 
@@ -283,6 +286,14 @@ fun AppNavHost() {
             )
         }
 
+        composable(
+            route = "game_directories"
+        ) {
+            GameDirectoriesScreen(
+                navigateBack = navController::navigateUp
+            )
+        }
+
         composable(
             route = "controls"
         ) {
@@ -546,7 +557,8 @@ fun AppNavHost() {
 fun GamesDestination(
     navigateToSettings: () -> Unit,
     navigateToControls: () -> Unit,
-    drawerState: androidx.compose.material3.DrawerState
+    drawerState: androidx.compose.material3.DrawerState,
+    navigateToDirectories: () -> Unit = {}
 ) {
     val context = LocalContext.current
     val scope = rememberCoroutineScope()
@@ -591,9 +603,11 @@ fun GamesDestination(
         contract = ActivityResultContracts.OpenDocumentTree(),
         onResult = { uri: Uri? ->
             uri?.let {
-                // TODO: FileUtil.saveGameFolderUri(prefs, it)
                 val takeFlags = Intent.FLAG_GRANT_READ_URI_PERMISSION
                 context.contentResolver.takePersistableUriPermission(it, takeFlags)
+                // Remember the folder so it can be re-scanned or removed later
+                // from the "Manage directories" screen.
+                GameDirectoryRepository.add(it)
                 FileUtil.installPackages(context, it)
             }
         }
@@ -722,6 +736,13 @@ fun GamesDestination(
                         onClick = navigateToControls
                     )
 
+                    NavigationDrawerItem(
+                        label = { Text(stringResource(R.string.manage_directories)) },
+                        selected = false,
+                        icon = { Icon(painterResource(id = R.drawable.hard_drive), contentDescription = null) },
+                        onClick = navigateToDirectories
+                    )
+
                     NavigationDrawerItem(
                         label = { Text(stringResource(R.string.device_info)) },
                         selected = false,
diff --git a/app/src/main/java/net/rpcsx/ui/settings/SettingsScreen.kt b/app/src/main/java/net/rpcsx/ui/settings/SettingsScreen.kt
index 9d8c0d2..afeaa93 100644
--- a/app/src/main/java/net/rpcsx/ui/settings/SettingsScreen.kt
+++ b/app/src/main/java/net/rpcsx/ui/settings/SettingsScreen.kt
@@ -100,6 +100,7 @@ import androidx.appcompat.app.AppCompatDelegate
 import androidx.core.os.LocaleListCompat
 import net.rpcsx.utils.FileUtil
 import net.rpcsx.utils.GamepadAutoMapper
+import net.rpcsx.utils.GeneralSettings
 import net.rpcsx.utils.InputBindingPrefs
 import net.rpcsx.utils.RpcsxUpdater
 import org.json.JSONObject
@@ -860,6 +861,31 @@ fun ControllerSettings(
                     onClick = navigateToTouchpad
                 )
             }
+
+            item {
+                val context = LocalContext.current
+                var forcedSlot by remember {
+                    mutableStateOf(
+                        ((GeneralSettings["overlay_forced_slot"] as? Int) ?: 0)
+                            .coerceIn(0, MaxGamepadPlayers - 1)
+                    )
+                }
+                val labels = (0 until MaxGamepadPlayers).map {
+                    context.getString(R.string.player_slot, it + 1)
+                }
+                SingleSelectionDialog(
+                    title = stringResource(R.string.touchpad_forced_port),
+                    subtitle = { Text(stringResource(R.string.touchpad_forced_port_desc)) },
+                    icon = null,
+                    currentValue = labels[forcedSlot],
+                    values = labels,
+                    onValueChange = { value ->
+                        val idx = labels.indexOf(value).coerceIn(0, MaxGamepadPlayers - 1)
+                        forcedSlot = idx
+                        GeneralSettings["overlay_forced_slot"] = idx
+                    }
+                )
+            }
         }
     }
 }
diff --git a/app/src/main/java/net/rpcsx/utils/FileUtil.kt b/app/src/main/java/net/rpcsx/utils/FileUtil.kt
index 5ca9b96..cc07097 100644
--- a/app/src/main/java/net/rpcsx/utils/FileUtil.kt
+++ b/app/src/main/java/net/rpcsx/utils/FileUtil.kt
@@ -38,6 +38,14 @@ private data class InstallableFolder(
 )
 
 object FileUtil {
+    // File extensions the folder scanner treats as installable game content.
+    private val installableExtensions = setOf("iso", "pkg", "pup", "edat", "rap")
+
+    private fun isInstallableFileName(name: String): Boolean {
+        val ext = name.substringAfterLast('.', "").lowercase()
+        return ext in installableExtensions
+    }
+
     fun installPackages(context: Context, rootFolderUri: Uri) {
         thread {
             val workList = mutableListOf<Uri>()
@@ -71,7 +79,9 @@ object FileUtil {
                 listFiles(currentFolderUri, context).forEach { item ->
                     if (item.isDirectory) {
                         workList.add(item.uri)
-                    } else {
+                    } else if (isInstallableFileName(item.filename)) {
+                        // Pick up loose installable files anywhere in the tree,
+                        // including .iso images, instead of every stray file.
                         batchFiles += item.uri
                     }
                 }
diff --git a/app/src/main/res/values/strings.xml b/app/src/main/res/values/strings.xml
index 5787ba3..201cb57 100644
--- a/app/src/main/res/values/strings.xml
+++ b/app/src/main/res/values/strings.xml
@@ -119,6 +119,10 @@
     <string name="key_mappings">Key Mappings</string>
     <string name="log_not_found">Log file not found!</string>
     <string name="manage_settings">Manage Settings</string>
+    <string name="manage_directories">Manage Directories</string>
+    <string name="no_directories">No directories added yet. Add a game folder to see it here.</string>
+    <string name="rescan_directory">Rescan</string>
+    <string name="remove_directory">Remove</string>
     <string name="no_activity_to_handle_action">No activity found to handle this action</string>
     <string name="output_scaling">Output Scaling</string>
     <string name="perform_input">Perform Input</string>
@@ -128,6 +132,8 @@
     <string name="share_log">Share Log</string>
     <string name="share_log_description">Share RPCSX\'s log file to help debug issues</string>
     <string name="touchpad">Touchpad</string>
+    <string name="touchpad_forced_port">Forced touchpad port</string>
+    <string name="touchpad_forced_port_desc">Which port the touch overlay uses when no physical controller is connected. With a controller connected the overlay always shares port 1.</string>
     <string name="users">Users</string>
     <string name="view_internal_dir">View Internal Directory</string>
     <string name="view_internal_dir_description">Open internal directory of RPCSX in file manager</string>
